name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Deploy to GitHub Pages
        run: |
          git config --global user.name 'kcqingfeng@gmail.ocm'
          git config --global user.email 'kcqingfeng@gmail.ocm'
          git remote add gh-pages git@github.com:akcshen/PortalWebsite.git
          git fetch gh-pages
          git push gh-pages master:gh-pages --force
